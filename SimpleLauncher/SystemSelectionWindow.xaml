<mah:MetroWindow x:Class="SimpleLauncher.SystemSelectionWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="{DynamicResource SelectRetroAchievementsSystemTitle}"
                 Height="220" Width="400"
                 WindowStartupLocation="CenterScreen"
                 ShowInTaskbar="False"
                 ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{DynamicResource SystemSelectionMessage}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10" />

            <TextBlock Text="{DynamicResource SystemSelectionPrompt}"
                       FontWeight="Bold"
                       Margin="0,0,0,5" />

            <ComboBox ItemsSource="{Binding Systems}"
                      SelectedItem="{Binding SelectedSystem}"
                      Margin="0,0,0,30"
                      ToolTip="{DynamicResource TooltipChooseSystemForGame}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="{DynamicResource ConfirmSelectionButton}"
                        Command="{Binding ConfirmCommand}"
                        IsDefault="True"
                        Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                        Margin="0,0,10,0"
                        Width="120"
                        ToolTip="{DynamicResource TooltipConfirmSelection}" />

                <Button Content="{DynamicResource Cancel}"
                        Command="{Binding CancelCommand}"
                        IsCancel="True"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"
                        Width="80"
                        ToolTip="{DynamicResource ToolTipCancel}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</mah:MetroWindow>