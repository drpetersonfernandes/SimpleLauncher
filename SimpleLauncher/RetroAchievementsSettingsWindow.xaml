<controls:MetroWindow x:Class="SimpleLauncher.RetroAchievementsSettingsWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      mc:Ignorable="d"
                      Title="{DynamicResource RetroAchievementsSettings}"
                      Width="500" Height="300"
                      ResizeMode="NoResize"
                      WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Username -->
        <Label Grid.Row="0" Grid.Column="0" Content="{DynamicResource Username}" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <!-- API Key -->
        <Label Grid.Row="1" Grid.Column="0" Content="{DynamicResource ApiKey}" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <PasswordBox x:Name="ApiKeyPasswordBox" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Hyperlink -->
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" TextWrapping="Wrap" Margin="0,10,0,0">
            <Run Text="{DynamicResource FindYourApiKeyHere}"/>
            <Hyperlink NavigateUri="https://retroachievements.org/controlpanel.php" RequestNavigate="Hyperlink_RequestNavigate">
                <Run Text="RetroAchievements Control Panel"/>
            </Hyperlink>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Save}" Click="SaveButton_Click" IsDefault="True" Width="80" Margin="0,0,10,0"/>
            <Button Content="{DynamicResource Cancel}" IsCancel="True" Width="80"/>
        </StackPanel>
    </Grid>
</controls:MetroWindow>