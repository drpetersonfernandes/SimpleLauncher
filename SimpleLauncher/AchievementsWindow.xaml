<controls:MetroWindow x:Class="SimpleLauncher.AchievementsWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      mc:Ignorable="d"
                      Title="RetroAchievements"
                      Width="800" Height="600"
                      WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="10" Padding="10" BorderBrush="Gray" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Image x:Name="GameCoverImage" Width="64" Height="64" Stretch="Uniform"/>
                <StackPanel VerticalAlignment="Center" Margin="15,0,0,0">
                    <TextBlock x:Name="GameTitleTextBlock" FontSize="20" FontWeight="Bold" Text="Game Title"/>
                    <TextBlock x:Name="ProgressTextBlock" FontSize="14" Margin="0,5,0,0" Text="Progress: 0/0 (0/0 Points)"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" Name="AchievementsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="10,0,10,10" CanUserAddRows="False" d:DataContext="{d:DesignInstance }">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Icon" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding BadgeUri}" Width="48" Height="48" Margin="5" d:DataContext="{d:DesignInstance }" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Points" Binding="{Binding Points}" Width="Auto"/>
                <DataGridTextColumn Header="Unlocked" Binding="{Binding DateUnlockedDisplay}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Grid.Row="0" Grid.RowSpan="2" Background="#7F000000" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <controls:ProgressRing IsActive="True" Width="60" Height="60" />
                <TextBlock Text="Loading Achievements..." Foreground="White" Margin="0,10,0,0" FontSize="16"/>
            </StackPanel>
        </Grid>
        
        <!-- No Results Overlay -->
        <Grid x:Name="NoResultsOverlay" Grid.Row="1" Background="{DynamicResource MahApps.Brushes.Control.Background}" Visibility="Collapsed">
            <TextBlock Text="No achievements found for this game, or the game could not be identified on RetroAchievements." VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" TextWrapping="Wrap" TextAlignment="Center" Margin="20"/>
        </Grid>
    </Grid>
</controls:MetroWindow>